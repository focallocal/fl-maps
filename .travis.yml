language: node_js
node_js:
- '0.10'
before_install:
- sudo apt-get install expect
- curl https://install.meteor.com | /bin/sh
- meteor update
- npm install
branches:
  only:
  - new-frontend
env:
  global:
  - METEOR_DEPLOY_URL=focallocal-test.meteor.com
script:
  - "expect deploy.exp"