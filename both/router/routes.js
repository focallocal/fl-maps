Router.route('/', {
  name: 'home'
});

Router.route('/events/calendar', {
  name: 'calendar'
});

Router.route('/events/new', {
  name: 'events.new'
});

Router.route('/events/map', {
  name: 'map'
});
